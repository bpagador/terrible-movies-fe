export const getMovies = state => state.movies;
// if other pieces of state are added beyond the current movies array, you will need to specify via state.VALUE which value of state you want to select
